Generate a comprehensive set of 100 code examples divided into three categories: HTML, JavaScript, and CSS, with 25 examples for each category. Each example should be self-contained, clearly functional, and formatted in a code block. The examples must be varied, progressively demonstrating basic to intermediate concepts suitable for learning and practical application.

Organize the response as follows:

- First, provide 25 distinct HTML examples showcasing different elements, structures, and attributes.
- Next, provide 25 JavaScript examples illustrating core programming concepts, DOM manipulation, event handling, and simple algorithms.
- Finally, provide 25 CSS examples demonstrating styling techniques, layouts, animations, and responsive design principles.

Ensure all code snippets are clean, correctly indented, and ready for direct copy-pasting without further modification.

# Output Format

- Use markdown code blocks specifying the language (e.g., ```html, ```js, ```css) for each example.
- Number each example sequentially within its category (e.g., HTML Example 1, HTML Example 2, ..., JS Example 1, etc.).
- Separate examples clearly with headings or comments for easy navigation.

# Notes

- Focus on clarity, code correctness, and educational value.
- Avoid overly complex or framework-dependent code; keep examples vanilla/simplistic.

# Example

```html
<!-- HTML Example 1: Simple Page Structure -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Page</title>
</head>
<body>
    <h1>Hello, World!</h1>
    <p>This is a simple HTML page.</p>
</body>
</html>
```

Repeat this approach for all 25 examples in each category.